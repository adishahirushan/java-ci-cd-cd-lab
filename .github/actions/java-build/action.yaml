name: "Java Build Action"
description: "Compile and run Java Program"

runs:
  using: "composite"
  steps:
    - name: Compile Java
      run: javac *.java
      shell: bash

    - name: Run Java Program
      run: java -cp . CalculatorTest
      shell: bash
