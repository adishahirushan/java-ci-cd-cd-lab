name: "java Build Action"

description: "Compile and run Java Program"

runs:
  using: "composite"

steps:
  - name: compile.java
    run: javac *.java 
    shell: bash

  - name: Run Java
    run: java Main
    shell: bash
